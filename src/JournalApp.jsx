import { AppRoutes } from './routes';
import { AppTheme } from './theme';

const JournalApp = () => {
  return (
    <AppTheme>
      <AppRoutes />
    </AppTheme>
  );
};

export default JournalApp;
